/*
 * conf/metacentrum.config
 *
 * Perfil preliminar para MetaCentrum.
 * Todavía no producción. Lo afinaremos más adelante.
 */

process {
    executor = 'pbspro'
}

executor {
    queueSize       = 20
    submitRateLimit = '10/1min'
}