/*
 * conf/test.config
 *
 * Perfil local de prueba.
 * Debe ser pequeño, rápido y seguro.
 */

params {
    input     = 'assets/samplesheet.test.csv'
    outdir    = 'results/test'
    test_mode = true
}

process {
    executor = 'local'
}